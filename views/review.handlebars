<div class="text-center">
  {{!-- <p>{{{get_status}}}</p> --}}
  <p>{{title}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    {{!-- <span {{get_status}} aria-label="status"></span> --}}
    <span class="status_medal">{{review}}</span>
  </p>
  {{!-- comment form  --}}
<div>
  <form class="form new-comment-form">
    <div class="form-group">
      <label for="comment-text">comment on this blog:</label>
      <textarea class="form-input" id="comment-text" title="comment-text"></textarea>
    </div>
    <div class="form-group">
      <button type="submit" class="btn btn-primary">Post Comment</button>
    </div>
  </form>
</div>
    {{! <button class="btn btn-primary">Show Reviews!</button> }}
    {{! like button  }}
    <button onclick="Toggle1()" id="btnh1" class="like-btn">
      <i class="fas fa-heart"></i>
    </button>
  </p>
  <div>{{comments.comment}}</div>
</div>
<div class="col-md-6 comment-list">
    <h5>Comments</h5>
     {{#each comments}}
    {{!-- {{#each blog.comments as |comment|}} --}}
      <div class="row mb-2 row-list">
        <div class="col-md-8">
          <p class="comment">{{comment}}</p>
          <div class="meta">
            {{user.name}}
            created on
            {{!-- {{#each comments}} --}}
            {{format_date date_created}}
          </div>
            {{!-- {{/each}} --}}
        </div>
      </div>
    {{!-- {{/each}} --}}
     {{/each}}
  </div>
<script src="/like/script.js"></script>
<script src="/js/comment.js"></script>
{{! <script src="/js/upvote.js"></script> }}