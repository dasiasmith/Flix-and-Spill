<div class="text-center border border-danger ">

  {{!-- <p>{{{get_status}}}</p> --}}
  <p class="text-size-1">{{title}}</p>
 
  <p class="text-size-2">
    {{!-- <span {{get_status}} aria-label="status"></span> --}}
    <span class="status_medal">{{review}}</span>
  </p>
   <p>created by {{user.name}} on {{format_date date_created}}</p>
   <button onclick="Toggle1()" id="btnh1" class="like-btn">
      <i class="fas fa-heart"></i>
    </button>
  {{!-- comment form  --}}
   {{#if logged_in}}
<div>
  <form class="form new-comment-form">
    <div class="form-group">
      <label for="comment-text"></label>
      <textarea class="form-input form-control" id="comment-text" title="comment-text"></textarea>
    </div>
    <div class="form-group">
      {{!-- <button type="submit" class="btn btn-primary">Post Comment</button> --}}
      <button
              type="submit"
              class="btn btn-outline-warning my-2 my-sm-0 search-btn"
              id="btn-post"
            >Add Comment</button>
    </div>
  </form>
</div>
  {{/if}}
    {{! <button class="btn btn-primary">Show Reviews!</button> }}
    {{! like button  }}
    
  </p>
  <div>{{comments.comment}}</div>
</div>

<div class="col-md-6 comment-list border border-danger">
    <h5 class="posted-comment">Posted Comments</h5>
 
     {{#each comments}}
    {{!-- {{#each blog.comments as |comment|}} --}}
      <div class="row mb-2 row-list">
        <div class="col-md-8">
          <p class="comment">{{comment}}</p>
          <div class="meta posted-comment comment-border">
            {{user.name}}
            created on
            {{!-- {{#each comments}} --}}
            {{format_date date_created}}
          </div>
          
            {{!-- {{/each}} --}}
        </div>
      </div>
    {{!-- {{/each}} --}}
     {{/each}}
  </div>
{{#if logged_in}}
<script src="/like/script.js"></script>
<script src="/js/comment.js"></script>
{{/if}}
{{! <script src="/js/upvote.js"></script> }}