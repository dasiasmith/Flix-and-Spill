<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>'Flix and Spill</title>
    <link rel="stylesheet" href="/css/jass.css" />
    <script
      src="https://kit.fontawesome.com/f6dcf461c1.js"
      crossorigin="anonymous"
    ></script>
    <link rel="stylesheet" href="/css/style.css" />
  </head>

  <body>
    <div class="flex-column justify-space-around">
      <header class="display-flex justify-space-between align-center p-2">
        <h1>
          <span role="img" aria-label="money">💸</span>
          <a href="/">Review a Show or Movie</a>
        </h1>
        <nav>

          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
            <a href="/profile">profile</a>
            |
            <button class="no-button" id="logout">logout</button>
          {{else}}
            <a href="/login">login</a>
          {{/if}}
          {{! search bar  }}
          <form class="form-inline my-2 my-lg-0" id="search-form">
            <input
              type="text"
              class="form-control mr-sm-2 bg-white text-dark"
              id="search-input"
              name="search-text"
              placeholder="Search"
              aria-label="Search"
            />
            <button type="submit" style="font-size:17px"><i
                class="fa fa-search"
              ></i></button>
            {{! <button
              type="submit"
              class="btn btn-outline-warning my-2 my-sm-0 search-btn"
              id="btn-post"
            >Search</button> }}
          </form>
        </nav>
      </header>
      <main class="container container-fluid mt-5">
        <!-- Render the sub layout -->
        {{{body}}}
      </main>
      <footer>

      </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
    {{! import search bar js code   }}
    <script src="/js/search.js"></script>
  </body>

</html>